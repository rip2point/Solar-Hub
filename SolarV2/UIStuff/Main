getgenv().SolarHubInfo = {
Version = "2.0.0",
Owner = "twistedworId",
Developer1 = "Jerhimibaseballpro",
Developer2 = "pianodano"
}

-- ui functions (if ur viewing this its just so the script isnt 1000000 lines of code)

function ButtonFunction(name, description, script)

end

function ToggleButton(name, description, script)

end

function TabButton(par1, par2, pos)

end
